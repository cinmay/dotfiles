#!/usr/bin/env bash

set -euo pipefail

# Use WSLg's Wayland socket explicitly. For [WARN:COPY MODE], see README.md.
export XDG_RUNTIME_DIR=/mnt/wslg/runtime-dir

nohup /usr/bin/chatgpt --ozone-platform=wayland "$@" \
  </dev/null >/dev/null 2>&1 &
