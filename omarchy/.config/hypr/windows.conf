# Terminal windows: float, center, and size relative to monitor.
windowrule = float on, match:tag terminal
windowrule = center on, match:tag terminal
windowrule = size (monitor_w*0.7) (monitor_h*0.98), match:tag terminal
